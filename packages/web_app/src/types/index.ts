export type ThemeType = {
    palette: {
        background: string,
        text: string,
        accent: string,
        shadow: string,
        link: string,
        informational: string,
        success: string,
        warning: string,
        error: string
    }, spacing: {
        toolbarTopHeight: number,
        toolbarLeftHeight: number
    }
}