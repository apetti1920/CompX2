//import { isPortStorageType } from "../../src/Network/BlockStorage";

describe("Port Storage Tests", () => {
    test("Is Port Storage", () => {
        // const port = { id: "12345", name: "p1", type: "number", initialValue: 0};
        // expect(isPortStorageType(port)).toBeTruthy();
        expect(1).toBe(1);
    });
});