{
  "name": "compx",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npx lerna run build",
    "start:electron": "npx lerna run --scope=@compx/electron_app build && npx lerna run start --scope=@compx/electron_app"
  },
  "keywords": [],
  "author": "Aidan Petti",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^16.11.7",
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.4.4"
  }
}
